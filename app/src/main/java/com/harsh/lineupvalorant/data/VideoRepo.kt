package com.harsh.lineupvalorant.data

/*

@Singleton
class VideoRepo @Inject constructor(private val videoDao: VideoDao) {

    fun getSearchResults(query: String) =
        Pager(
            config = PagingConfig(
                pageSize = 10,
                maxSize = 50,
                enablePlaceholders = false
            ),
            pagingSourceFactory = {
                VideoDataSource(videoDao, query)
            }
        )
}*/
